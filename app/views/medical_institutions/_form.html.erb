<%= form_for(@medical_institution) do |f| %>
  <% if @medical_institution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medical_institution.errors.count, "error") %> prohibited this medical_institution from being saved:</h2>

      <ul>
      <% @medical_institution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :region %><br>
    <%= f.text_area :region %>
  </div>
  <div class="field">
    <%= f.label :x_coord %><br>
    <%= f.text_field :x_coord %>
  </div>
  <div class="field">
    <%= f.label :y_coord %><br>
    <%= f.text_field :y_coord %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
